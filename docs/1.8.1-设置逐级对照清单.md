# AT Music 1.8.1 设置逐级对照清单

目标：开发版的设置层级、标题、展开方式、控件位置与原版 1.8.1 一致；NAS、聚合和本地音乐只作为原版层级内的扩展项，不替换原版入口。

规则：每一项必须依次完成「原版拍屏/读取 → 开发版界面比对 → 功能实际操作 → 真机返回与重开验证」。只有四项都通过才标记 ✅；仅有代码或仅能编译均为 🟡。

## 1. 设置首页

- 🟡 顶部半屏弹层、拖拽条、标题、完成按钮、搜索框、紧凑分组。
- 🟡 搜索设置：一级项目、已展开子项和深层控件均可按关键词命中。

## 2. 账号与平台

- 🟡 账号登录 → 网易云 → 扫码/网页登录、账号状态、歌单同步。
- 🟡 账号登录 → QQ 音乐 → 扫码/网页登录、账号状态、歌单同步。
- 🟡 账号登录 → 酷狗 → 扫码登录、账号状态、歌单同步。
- 🟡 账号登录 → 群晖 NAS（扩展）→ 地址、登录、曲库索引、退出。
- 🟡 平台显示 → 平台勾选、排序、聚合与单平台入口的可见性。

## 3. 主题设置

- ✅ 主题模式 → 跟随系统、浅色、深色三张独立选择卡。
- ✅ 主题模式 → 关闭液态效果：开启时切换为无液态玻璃的 Apple 简洁风格，关闭时恢复原生液态玻璃与流动背景效果，全局组件及页面即时联动并持久化。
- ✅ 主题模式 → 更多外观与主页设置：增加独立展开按钮，将关闭液态效果下方的语言、底栏文字、VIP 标识、全局 UI 样式、强调色、背景色/壁纸、文字颜色、模糊度、主页元素隐藏及字体设置统一收纳折叠，展开后可见完整设置。
- 🟡 主题模式 → 主题强调色：设置图标、底栏选中态、搜索选中态、公告图标和布局编辑器控件统一联动；红色只保留删除、退出和错误状态。
- 🟡 主题模式 → 全局 UI 样式、主题预设、自定义强调色、恢复预设。
- 🟡 主题模式 → 浅色/深色独立背景色、壁纸、同步范围、壁纸模糊度。
- 🟡 主题模式 → 底栏文字、VIP 标识、主页标题与排序入口、字体与问候语。

## 4. 运行环境与显示开关

- 🟡 运行环境 → 设备、系统、界面尺寸、应用版本。
- 🟡 运行环境 → 关闭动态效果、隐藏右上角外观切换、隐藏自愿赞助。

## 5. 音源与音质

- 🟡 音源与音质 → 统一音质、Wi-Fi/蜂窝质量选择、第三方音源管理。
- 🟡 音源与音质 → 失败自动切换同名歌曲、音源状态与错误提示。

## 6. 播放设置

- 🟡 播放设置 → 与其他音频同时播放、120Hz、恢复上次播放、触感反馈、会员歌提醒。
- 🟡 播放设置 → 播放器设置 → 三种播放器风格、布局编辑器、真实当前歌曲预览。
- 🟡 播放设置 → 歌词、评论、队列、睡眠定时、下载与歌曲信息入口。

## 7. 均衡器与备份

- 🟡 均衡器 → 开关、预设、频段调节、重启后保存。
- 🟡 备份与恢复 → 登录信息、壁纸选项、导出、导入、覆盖确认。
- 🟡 更新日志、检查更新与版本号展示。

## 当前批次记录

- 1.8.1 (8)：补齐“关闭液态效果”，并使其影响通用玻璃容器、动态背景、播放器液态按钮和播放器设置面板。
- 1.8.1 (9)：已统一设置、底栏、搜索、首页公告、个人页和播放器布局编辑器中写死的通用红色；Debug 与 Release 均编译通过，并已安装到真机。
- 1.8.1 (10)：
  1. 底栏导航栏与搜索圆形按钮全面适配原生液态玻璃与动态液态放大动效，支持滑动/点击弹簧放大与浮动。
  2. 设置页与歌单列表滚动性能深度优化：移除高开销嵌套实时模糊渲染，封面图优先命中内存缓存跳过二次刷新，歌单分组算法单次 O(N) 遍历。
  3. 各单平台主页排行榜统一为聚合主页的横向大卡片流展示。
- 1.8.1 (11)：
  1. 四套全局 UI 样式（默认液态、磨砂玻璃、紧凑淡雅、Apple 简洁）深度统一：ATMusicGlass 与 ATMusicSurface 统一处理各风格的基底材质、边缘反光、高光渐变与阴影。在 iOS 16+ 全版本设备上，液态模式均具备清透基底、液态渐变光晕与晶莹边缘反光，彻底解决真机无液态质感的问题。
  2. 全局各主页面（主页、精选、歌单、我的）全面接入统一 UI 风格系统，卡片、网格、搜索栏与分类药丸在四种样式间切换时全局统一联动。
  3. 歌曲播放页面深度匹配：播放/暂停与切歌等控制按钮底座、播放设置面板、播放队列与操作弹窗全部同步四个全局 UI 样式。
