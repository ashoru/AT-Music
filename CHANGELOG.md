# AT Music 更新日志

## 1.8.7 build 38

### 音乐库与全部歌单

- 歌单管理 Sheet 新增明确“完成”退出按钮，不再只能下拉刷新、无法返回。
- “全部歌单”顶部常驻“搜索歌单名称”，大量歌单可直接按名称匹配。
- 全部歌单排序改为：本地红心/收藏歌单 → 各平台我喜欢/红心歌单 → 其它歌单按最近打开/使用排序。
- 为歌单记录最近活动时间，打开歌单后会参与后续排序。
- 移除歌单行手写 chevron，只保留 NavigationLink 系统箭头，修复最右侧出现两个“>”。

### 本地标签与封面

- 本地歌曲标签保存改为显式持久化并读回校验，不能读回时不再提示成功。
- 标签更新同步到导入歌曲库、所有本地歌单、当前播放队列、播放历史和系统 Now Playing。
- 自定义封面每次使用新的受管文件 URL，避免图片缓存继续显示旧封面；新封面文件会在保存前后校验。
- 本次修复针对 AT Music 管理的本地副本/应用内元数据一致性；不会修改用户在“文件”App 中最初选择的源文件。

### 连续播放与下一首缓存

- 第三方音源进入 playing 后立即启动下一首预缓存，不再只依赖延迟 1.2 秒的单一确认回调。
- 播放器创建当前项目后也会主动刷新下一首预缓存目标，MusicCacheManager 自动去重。
- 第三方直链在曲末未正常发出 AVPlayerItemDidPlayToEndTime 时，增加曲末延迟校验兜底并自动切到下一首。
- 自动下一首使用 loadGeneration 防重复，同一首不会被系统结束通知和兜底逻辑重复推进。
- 所有音源重试均失败后的自动跳下一首等待由 10 秒缩短到 2 秒。

### NAS 文件浏览性能

- Synology 文件夹列表加入按服务器/目录隔离的 Library/Caches 持久化缓存。
- 5 分钟内新鲜缓存直接返回；24 小时内旧缓存先展示、随后后台刷新。
- 大目录不再一次请求 500 条，改成每页 120 条，首批返回即可显示，后续页渐进补齐。
- 下拉刷新会强制失效当前目录缓存并重新分页加载。
- NAS 未连接时继续阻止无意义网络请求。


## 1.8.6 build 37

### 音乐库重构

- 底部“歌单”改为“音乐库”，不再把多平台歌单、本地音乐、最近播放和 NAS 全部堆在一个长页面里。
- 音乐库首屏只保留 3 个浏览入口：最近播放、本地音乐、NAS 文件；下面最多显示 8 个我的歌单。
- 首页歌单按来源轮询展示，避免单一平台占满首屏。
- 多平台歌单缓存优先、渐进并发刷新；任一平台先完成就先更新，不等待最慢来源。
- “全部歌单”下沉到二级页，支持全部 / 本地 / 网易云 / QQ / 酷狗 / NAS 筛选。
- 原复杂歌单页保留为“歌单管理”，只从二级页的更多菜单进入。

### 本地音乐浏览

- 本地音乐新增统一浏览结构：文件 / 专辑 / 歌手 / 本地歌单；文件页显示导入时的真实原始文件名。
- 保留导入音乐、新建本地歌单、管理本地音乐功能，统一收进右上角更多菜单。
- 文件、专辑/歌手歌曲页和本地歌单详情支持播放全部、随机播放和搜索。
- 当前本地导入存储会重命名为 ATMusicAudio/aud_xxx，因此不会伪造原始目录树；浏览结构基于真实歌曲标签组织。

### NAS 文件浏览

- NAS 文件提升为音乐库一级入口，直接进入 Audio Station 文件夹树。
- 文件夹始终排在歌曲前面；支持无限递归进入子文件夹。
- 每个目录支持当前目录搜索、播放全部和随机播放。
- NAS 未连接时显示明确连接提示，不触发无意义加载。

### 歌单详情瘦身

- 封面缩为 80pt，首屏只保留歌单名、来源、创建者和歌曲数。
- 搜索改为右上角“更多”中的按需展开，不再永久占首屏。
- 排序改到右上角“更多”菜单。
- 简介与播放热度移入独立“歌单信息”页。
- 首屏优先展示歌曲内容，减少大块 Surface 和控制区占用。


## 1.8.5 build 36

### 播放队列

- 编辑模式不再使用队列 offset 作为 SwiftUI 行身份，改为歌曲 identityKey + 同曲出现序号，避免播放器进度刷新时整行反复重布局。
- 编辑期间关闭列表和歌曲行的外部隐式动画。
- 当前播放歌曲的 waveform 在编辑模式下改为静态图标，避免 repeating symbolEffect 干扰布局。
- 编辑 / 完成、清空队列、点击队列歌曲补充一致的触感反馈。

### 播放按钮反馈

- GlassButton 统一内置触感反馈：主操作使用 medium，普通操作使用 light tap。
- iOS 26/27 下四套 UI 都有明确按压视觉反馈，不再只依赖 Liquid Glass 的 interactive 效果。
- 歌单、排行榜、NAS、本地歌单和搜索中的播放全部/随机播放统一补齐按压反馈。

### 四套 UI 选中态

- 默认液态：系统 Liquid Glass + 主题色轻染。
- 磨砂玻璃：磨砂层 + 轻主题描边。
- 紧凑淡雅：低对比主题色浅底。
- Apple 简洁：系统主色/填充的极简选中态。
- 选中文字/图标也通过 atmusicSelectionForeground 跟随当前 UI 风格，不再统一硬编码白色或同一种主题色。


## 1.8.4 build 35

### 主页统一

- 网易、QQ、酷狗统一首页结构和推荐卡样式；网易移除独立特殊主页分支。
- 网易第一排恢复“每日推荐 + 红心雷达”，私人漫游随后展示。
- 平台切换在所有 UI 风格直接显示；标题本身也可打开平台菜单。
- 默认板块顺序简化为：推荐 → 歌单 → 排行榜。
- NAS 首页调整为：今日推荐 → 歌单 → 最近播放 / 喜欢 → 专辑 / 歌手，并缩短首次加载阻塞。

### 加载性能

- 首页快照从仅内存缓存升级为可丢弃磁盘缓存；冷启动优先展示上次内容，再后台更新。
- 网易 / QQ / 酷狗单平台首页拆成并发渐进加载，任一板块先返回即可先显示。
- 聚合主页保持分平台并发加载，切换平台时清理旧平台快照，避免错页和闪屏。
- 清理已无 UI 入口的网易歌单分类状态和加载死代码。

### 选中态与可读性

- 新增统一 ATMusicSelectableSurface，主页、搜索、音乐库、播放队列、外观/音质/均衡器和播放器布局选择器统一选中态。
- 选中态统一为主题色轻底 + 主文字色 + 细描边，不再使用浅背景白字。
- 移除残留系统 borderedProminent 按钮，避免系统前景色与 AT Music 主题冲突。
- 播放全部、聚合等按钮统一使用 AT Music 自己的高亮表面。


## 1.8.3 build 34

### 新增与优化

- 完整移除汽水音乐平台相关功能与入口。
- 网易云主页第一排恢复“红心雷达”，与“每日推荐”并排显示。
- 首页左上角平台切换由长按改为直接点击标题/下拉图标。

### 修复与调整

- 首次安装或无缓存时，聚合主页不再等待全部平台返回；任一平台先返回有效内容即可立即展示，后续结果继续补齐。
- 清理汽水音乐在搜索、播放、歌词、下载、收藏、歌单、设置和统一来源模型中的残留逻辑。


## 1.8.2 build 33

### 新增与优化

- 当前歌曲稳定播放后自动提前缓存真实播放队列中的下一首，切歌优先读取本地缓存。
- 音乐缓存上限可在设置中选择 500 MB、1 GB、2 GB、5 GB、10 GB 或 20 GB，默认 2 GB。
- 缓存使用 LRU 自动淘汰，并在设备剩余空间较低时主动回收；支持一键清除。

### 修复与调整

- 队列插队、重新排序、随机播放切换后会重新计算下一首预缓存目标。
- 单曲循环不重复预缓存下一首。
- 缓存损坏或被系统清理时自动回退网络播放。


## 1.6.4

### 新增与优化

- 优化本地播放、主题和播放器设置体验。

### 修复与调整

- 整理公开客户端工程结构，保持客户端功能独立。

## 1.5.8

### 新增与优化

- 新增网易云音乐、QQ音乐、酷狗音乐歌单一键同步到本地。
- 本地歌单支持编辑和搜索，批量选择歌曲后可添加到其他本地歌单。
- 新增 QQ 音乐热门歌单展示。
- 主页问候语支持自定义文字、颜色、大小、发光、专属字体、底部横线和上下渐变，并可逐行选择渐变颜色。
- 播放器支持左右滑动切换歌曲，新增顶部三平台排序、隐藏主页刷新/用户名/排序按钮。
- 播放器按钮图标样式新增，播放器设置界面重新整理分组和控件排版。
- 主页、音乐库、我的、设置页增加 iPad 最大宽度适配。
- 播放列表、最近播放和日志界面背景同步主页壁纸。

### 修复与调整

- 修复 QQ 音乐喜欢列表不显示的问题。
- 修复最近播放、日志、本地歌单和播放列表不同步主页壁纸的问题。
- 修复设置页掉帧问题；如果本次仍然掉帧，建议更换设备。
- 修复 iOS 15 编译兼容问题。

## 1.5.6

### 新增与优化

- 封面页歌名、歌手和预览歌词支持渐变、高光及高光强度调节。
- 播放页背景浮尘新增开关，默认关闭；动态浮尘支持密度和大小调节。
- 全局上传壁纸自动同步到播放器封面页背景。
- 播放页支持从顶部下划关闭，并加入缩放、淡出动画。

### 修复与调整

- 修复酷狗排行榜歌曲封面缺失或未归一化的问题。
- 提高内置音源搜索上限。
- 播放器设置打开后不再默认展开“播放”和“歌词显示”分组。
- 修复部分自签安装后启动即退出的问题：将系统媒体服务、字体注册和壁纸恢复移出 App 初始化阶段，并保留播放时的系统音频初始化兜底。

## 1.5.5

### 重要提示

> <span style="color:#ff7a00;font-weight:700;">从 1.5.4 版本开始，播放器设置已从右上角删除，改为点击中间歌曲正在播放的标题打开。</span>

### 新增与优化

- 优化播放中全局刷新策略，移除高刷保持器的常驻空转刷新，降低设置页、我的页面和播放器页面的发热与掉帧。
- 本地壁纸、歌词背景、设置页缩略图改为复用解码缓存，减少滚动和切换设置时的重复图片解码。
- 锁屏/系统正在播放封面增加缓存，避免播放状态变化时反复下载和刷新同一张封面。
- 聆澜内置音源支持多密钥池，当前密钥未命中时自动切换下一个，并记住最近可用密钥。
- 播放器设置新增封面页歌名、歌手、预览歌词与未播放歌词颜色调节。

### 修复

- 修复播放中进度更新过于频繁导致非播放器页面也跟随重绘的问题。
- 修复重新上传歌词背景或恢复壁纸后，部分位置可能继续显示旧图片缓存的问题。
- 修复酷狗排行榜详情歌曲封面链接未归一化，并在官网榜单缺封面时自动用移动端榜单数据补齐封面。
- 优化巨魔安装场景下播放中切换页面的刷新与解码负担。
